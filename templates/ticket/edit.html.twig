{% extends 'base.html.twig' %}

{% block title %}Éditer la tâche{% endblock %}

{% block body %}
    <h1>Éditer la tâche</h1>

    {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}

    

    {{ include('ticket/_delete_form.html.twig') }}
    <a aria-label="Retour à la liste des tâches" href="{{ path('app_ticket_index') }}">Retour à la liste</a>
{% endblock %}
